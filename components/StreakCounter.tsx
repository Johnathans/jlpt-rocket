'use client';

import { useState } from 'react';

export default function StreakCounter() {
  const [streak, setStreak] = useState(7); // Example streak count
  const [progress, setProgress] = useState(65); // Example progress percentage

  return (
    null
  );
}